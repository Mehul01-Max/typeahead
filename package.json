{
  "name": "search-typeahead",
  "version": "1.0.0",
  "description": "Search typeahead system with distributed caching and trending searches",
  "main": "server/index.js",
  "type": "module",
  "scripts": {
    "start": "node server/index.js",
    "seed": "node scripts/seed.js",
    "dev": "node --watch server/index.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "csv-parser": "^3.0.0",
    "dotenv": "^17.4.2",
    "express": "^4.18.2",
    "pg": "^8.22.0",
    "redis": "^4.6.12"
  }
}
